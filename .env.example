# ===========================================
# CONFIGURACIÓN DE LA APLICACIÓN
# ===========================================
# NODE_ENV=development          # Opcional: local, development, prod (default: development)
# PORT=3000                     # Opcional: Puerto HTTP (default: 3000)
# ENABLE_SWAGGER=false          # Opcional: Habilitar Swagger (default: false)

# ===========================================
# CONFIGURACIÓN DE BASE DE DATOS (PostgreSQL)
# ===========================================
# DB_HOST=localhost             # Opcional: Host de la base de datos (default: localhost)
# DB_PORT=5432                  # Opcional: Puerto PostgreSQL (default: 5432)
# DB_USERNAME=postgres          # Opcional: Usuario de la base de datos (default: postgres)
# DB_PASSWORD=postgres          # Opcional: Contraseña de la base de datos (default: postgres)
# DB_DATABASE=auth_service      # Opcional: Nombre de la base de datos (default: auth_service)
# DB_SSL=false                  # Opcional: Habilitar SSL (default: false)

# ===========================================
# CONFIGURACIÓN DE SEGURIDAD JWT
# ===========================================
# JWT_SECRET=your-super-secret-jwt-key-change-in-production  # Opcional: Clave secreta JWT (default: development-secret-key-change-in-production)
# JWT_EXPIRES_IN=1h            # Opcional: Tiempo de expiración (default: 3600s)

# ===========================================
# CONFIGURACIÓN DE CORS
# ===========================================
# CORS_ORIGIN=http://localhost:3000,http://localhost:3001  # Opcional: Orígenes permitidos (default: http://localhost:3000)

# ===========================================
# CONFIGURACIÓN DE LOGGING
# ===========================================
# LOG_LEVEL=info                # Opcional: debug, info, warn, error (default: info)

# ===========================================
# CONFIGURACIÓN DE RATE LIMITING
# ===========================================
# RATE_LIMIT_WINDOW_MS=60000   # Opcional: Ventana de tiempo en ms (default: 60000)
# RATE_LIMIT_MAX_REQUESTS=200  # Opcional: Máximo de requests (default: 200)

# ===========================================
# NOTAS:
# ===========================================
# - Todas las variables son opcionales ya que Joi proporciona valores por defecto
# - Los valores por defecto se toman de src/config/config.default.ts
# - Para modificar valores por defecto, edita directamente config.default.ts
# - Para desarrollo, solo necesitas configurar las variables que quieras personalizar
# - Para producción, se recomienda configurar JWT_SECRET y credenciales de BD
